/** High-intent routes prefetched after idle (avoid competing with LCP). */
export const PREFETCH_ROUTES = [
  "/",
  "/shop",
  "/sell",
  "/warranty",
  "/support",
  "/contact",
  "/about",
  "/privacy-policy",
  "/terms-of-service",
] as const;

export const NAVBAR_OFFSET = 68;
