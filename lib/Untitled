Generate a COMPLETE production-ready Google Apps Script backend and frontend integration system for my Next.js ecommerce website.

Goal:

Every form on the website should be able to send data to Google Sheets reliably.

Generate:

1. Google Apps Script (Code.gs)
2. Frontend submitForm service
3. Environment configuration
4. Error handling
5. Validation
6. Future-proof architecture

========================================
GOOGLE APPS SCRIPT
==================

Generate a complete Code.gs file.

Requirements:

* Handle POST requests
* Accept JSON payloads
* Parse request body safely
* Append rows to Google Sheet
* Return JSON response
* Handle errors properly
* Include timestamps
* Support multiple form types

Fields:

timestamp
formType
name
phone
email
message
page
source

Use:

doPost(e)

Return:

{
success: true,
message: "Form submitted successfully"
}

or

{
success: false,
message: "Error message"
}

========================================
MULTI-FORM SUPPORT
==================

Support:

* Contact Form
* Product Inquiry
* Warranty Registration
* Newsletter
* Service Request
* Future Forms

Use formType field.

Example:

formType = "contact"

formType = "product_inquiry"

formType = "newsletter"

The Apps Script should automatically process all form types.

========================================
GOOGLE SHEET STRUCTURE
======================

Create rows:

Timestamp
Form Type
Name
Phone
Email
Message
Page URL
Source

Always append.

Never overwrite.

========================================
SECURITY
========

Add:

* input sanitization
* empty field checks
* malformed request handling

Prevent crashes.

========================================
FRONTEND SERVICE
================

Generate:

lib/forms/submitForm.ts

Reusable function:

submitForm(data)

Should:

* call Apps Script endpoint
* send JSON
* handle errors
* return response
* support all forms

========================================
FORM HOOK
=========

Generate:

hooks/useFormSubmit.ts

Features:

* loading state
* success state
* error state
* reusable

========================================
USER EXPERIENCE
===============

During submit:

* disable button
* show spinner
* prevent double submit

After success:

* clear form
* show toast

After failure:

* show error message

========================================
ENVIRONMENT VARIABLES
=====================

Generate:

.env.local

Example:

NEXT_PUBLIC_FORM_ENDPOINT=

Frontend should read endpoint from env.

========================================
GOOGLE APPS SCRIPT DEPLOYMENT
=============================

Explain:

1. Create Apps Script
2. Paste Code.gs
3. Connect Sheet
4. Deploy Web App
5. Anyone access
6. Copy Web App URL
7. Add URL to env

========================================
NEXT.JS COMPATIBILITY
=====================

Must work with:

* Next.js App Router
* TypeScript
* Vercel Deployment
* Production Domain
* localhost

========================================
ERROR LOGGING
=============

Add:

Logger.log()

for debugging.

Log:

* request received
* data received
* row inserted
* errors

========================================
FINAL OUTPUT
============

Generate:

1. Complete Code.gs
2. Complete submitForm.ts
3. Complete useFormSubmit.ts
4. Example ContactForm integration
5. Deployment instructions

Everything should be production-ready and reusable for any future form added to the website.
