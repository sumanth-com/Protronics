/** Re-export structured data builders (implementation in lib/local/schema.ts). */
export {
  buildGlobalJsonLdGraph,
  buildLocalBusinessJsonLd,
  buildLocationPageJsonLd,
  buildOrganizationJsonLd,
  buildReviewJsonLd,
  buildServiceJsonLdList,
  buildWebSiteJsonLd,
} from "@/lib/local/schema";
